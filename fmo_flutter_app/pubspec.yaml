name: fmo_mobile_controller
description: FMO Mobile Controller Flutter App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.0.0
  web_socket_channel: ^2.4.0
  shared_preferences: ^2.2.0
  permission_handler: ^11.0.0
  fftea: ^0.4.0
  sound_stream: ^0.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
